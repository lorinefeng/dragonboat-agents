# Task Prompt

Review the local command deck UI at `http://127.0.0.1:5173`.

Before launching visual rowers, run `dragonboat browser doctor`. If browser access or multimodal routing is unhealthy, block instead of downgrading.

If healthy, launch one visual QA rower to capture desktop and mobile screenshots, identify layout problems, and submit evidence with screenshot paths.

The steerer should fix small pinpoint CSS issues directly instead of launching a crew for each minor visual correction.
